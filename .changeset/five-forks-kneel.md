---
"nutstore-webdav-secret-cli": minor
---

Fix the compiled CLI runtime so the npm-distributed `dist` entry boots reliably with OpenTUI preload and advances past stored-cookie initialization like the development entrypoint.
